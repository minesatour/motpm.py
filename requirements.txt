# Core dependencies for the script
mitmproxy==10.2.4        # For intercepting HTTPS traffic and capturing OTPs
selenium==4.18.1         # For controlling the Chromium browser
webdriver-manager==4.0.1 # For automatically downloading Chromedriver
certifi==2024.2.2        # For CA certificate verification
urwid==2.1.2

# Note: tkinter is typically included with Python, but if not, install via system package (e.g., sudo apt install python3-tk)
